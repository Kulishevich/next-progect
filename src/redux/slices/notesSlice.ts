import { createSlice } from "@reduxjs/toolkit";

type NoteType = {
    
}

const initialState = {

}

const notesSlice = createSlice({
    name: 'notes',
    initialState: {},
    reducers: {

    }
})